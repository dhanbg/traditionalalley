# Next.js frontend service has been migrated to Vercel and is no longer run on the VPS via Docker.
# The production site is hosted on Vercel at https://traditionalalley.com.np.
# The VPS only hosts the PostgreSQL database and Strapi backend.
